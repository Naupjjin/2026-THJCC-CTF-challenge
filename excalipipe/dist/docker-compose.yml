version: '3.3'

services:
  pwn-kernel-excalipipe:
    build:
        context: .
    ports:
      - '13371:13371'
    restart: unless-stopped
    deploy:
      resources:
        limits:
          cpus: '4'
          memory: 1000M  
